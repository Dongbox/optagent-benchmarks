name: Publish Results Index

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "benchmark.py"
      - "benchmarks/presentation/results/**"
      - "benchmarks/presentation/aggregates/**"
      - "benchmarks/presentation/generate_dashboard_data.py"

permissions:
  contents: write
  actions: write

jobs:
  publish-index:
    name: Regenerate Dashboard Data
    runs-on: ubuntu-latest
    steps:
      - name: Checkout optagent-benchmarks
        uses: actions/checkout@v7
        with:
          path: benchmarks

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Regenerate index and aggregates
        working-directory: benchmarks
        run: python benchmark.py generate-results-index

      - name: Commit generated dashboard data
        working-directory: benchmarks
        run: |
          if git diff --quiet -- benchmarks/presentation/results/index.json benchmarks/presentation/aggregates; then
            echo "Generated dashboard data is already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add benchmarks/presentation/results/index.json benchmarks/presentation/aggregates
          git commit -m "Regenerate benchmark dashboard data"
          git push

      - name: Dispatch dashboard deploy
        env:
          GH_TOKEN: ${{ secrets.OPTAGENT_DASHBOARD_WORKFLOW_TOKEN }}
        run: |
          if [ -z "${GH_TOKEN}" ]; then
            echo "OPTAGENT_DASHBOARD_WORKFLOW_TOKEN is not set; skipping dashboard dispatch."
            exit 0
          fi
          gh workflow run deploy-dashboard.yml \
            --repo Dongbox/optagent-dashboard \
            --ref main \
            -f benchmark_ref="${GITHUB_REF_NAME}"
