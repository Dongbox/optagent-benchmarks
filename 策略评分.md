# 策略评分 (Strategy Score)

> 版本：v1.0  
> 日期：2026-07-06  
> 用途：CI 回归检测、快速性能比较

## 目的

策略评分是一个**轻量级回归指标**，用于快速判断策略修改是否改善了优化性能。为 CI/PR 流程提供快速反馈，输出单一标量分数用于接受/拒绝决策。

**适用场景：**
- PR 回归检测
- 策略版本快速对比
- 开发迭代的即时反馈

**不适用于：**
- 深入的研究级评估（见 `评估框架.md`）
- 算子级性能分析
- 统计显著性检验

## 设计原则

1. **时间稳定** — 评分在数月内保持可比性
2. **快速计算** — 每个实例单次运行即可，无需多 seed
3. **对真实改进敏感** — 能检测到有意义的变化，忽略噪声
4. **低方差** — 同一代码多次运行变化极小

## 评分定义

```
策略评分 = 
  0.40 × 解质量分 (D1) +
  0.30 × 任意时刻分 (D2) +
  0.15 × 效率分 (D3) +
  0.15 × 稳定性分 (D4)
```

范围：0–100（越高越好）

### D1：解质量分 (Solution Quality) — 40%

衡量最终解相对于参考成本的质量。

```python
gap_rel = (objective - reference_cost) / |reference_cost|
quality_score = max(0, 100 × (1 - gap_rel / gap_threshold))
```

**输入：**
- `objective` - 最终解的目标函数值
- `reference_cost` - 最优已知解（BKS）或套件最优
- `gap_threshold` - 问题类型相关

**gap_threshold 设定：**

| 问题组 | gap_threshold | 依据 |
|--------|:-------------:|------|
| routing (TSP/VRP) | 0.20 | TSPLIB 中 20% gap 为中等表现 |
| scheduling | 0.50 | Job Shop benchmark 中难度通常更高 |
| assignment (QAP) | 0.30 | QAPLIB 中等难度实例的典型 gap |

**规则：**
- 不可行解 → 0 分
- 达到或优于 BKS（gap ≤ 0）→ 100 分
- 缺失 BKS → 使用套件最优，标记 `reference_type: "relative"`

### D2：任意时刻分 (Anytime Performance) — 30%

衡量搜索**全过程**的解质量，而非仅看最终结果。

**Primal Integral 定义：**

```python
primal_integral = ∫₀ᵀ gap(t) dt

其中 gap(t) = max(0, (incumbent(t) - reference_cost) / |reference_cost|)
```

incumbent(t) 为阶梯函数，仅在解改进时更新。

**归一化评分：**

```python
max_integral = gap_threshold × T  # 最坏情况
anytime_score = max(0, 100 × (1 - primal_integral / max_integral))
```

**输入：**
- `incumbent_trace` - 改进事件列表 `[(elapsed_s, objective), ...]`
- `time_budget` - 总运行时间 T
- `reference_cost` - 参考成本

**规则：**
- 早期即达到好解且全程保持 → 高分
- 仅后期突然变好 → 被惩罚（相比仅看最终 objective 的 D1）
- 全程未改进 → 低分

### D3：效率分 (Runtime Efficiency) — 15%

衡量搜索吞吐率。

```python
evaluations_per_s = total_candidates_evaluated / wall_time_seconds
efficiency_score = min(100, 100 × log₁₀(eval_per_s) / log₁₀(expected))
```

**输入：**
- `candidates_evaluated` - 探索的候选解总数
- `wall_time_seconds` - 总运行时间
- `expected_throughput` - 基线吞吐率

**expected_throughput 基线：**

| 问题组 | 基准 (eval/s) | 依据 |
|--------|:-------------:|------|
| routing | 100,000 | 中等规模 TSP/VRP（release 构建） |
| scheduling | 10,000 | 调度问题计算密集 |
| assignment | 50,000 | QAP 中等规模 |

**规则：**
- 使用对数刻度（不同算子 evaluation 成本差异巨大）
- 上限 100 分（超过基线不额外加分）
- 吞吐率低于基线 10% → 疑似性能问题

### D4：稳定性分 (Stability) — 15%

衡量运行间一致性（简化版，用于单 seed 代理）。

**单次运行代理指标：**

```python
# 基于终止质量的简化稳定性评估
termination_quality = {
  "converged": 100,
  "optimal": 100,
  "diversity_exhausted": 80,
  "time_limit": 60,
  "iteration_limit": 40,
  "stagnation": 20,
}[termination_reason]

stability_score = termination_quality
```

**输入：**
- `termination_reason` - 运行结束方式

**注意：** 完整的多 seed 稳定性分析（CV、最差情况控制）属于评估框架，不在 CI 评分中。

## 评分输入

所有输入必须从单次运行中获得：

| 输入 | 来源 | 类型 |
|------|------|------|
| `objective` | `SolveResult.objective` | float |
| `reference_cost` | benchmark 元数据 | float |
| `incumbent_trace` | `diagnostics["incumbent_trace_json"]` | list[(float, float)] |
| `time_budget` | 配置 | float |
| `wall_time_seconds` | `diagnostics["wall_time_seconds"]` | float |
| `candidates_evaluated` | `diagnostics["ga_offspring_evaluated"]` 或 `candidates_evaluated` | int |
| `termination_reason` | `diagnostics["termination_reason"]` | string |

## 聚合

### 实例评分（Per-Instance）
同一 benchmark_id 多次运行的平均（如有）：
```python
instance_score = mean([run.composite_score for run in runs])
```

### 组评分（Per-Group）
一个 benchmark_group 下所有实例的平均：
```python
group_score = mean([instance.composite_score for instance in group])
```

### 策略评分（Strategy Score，全局）
跨所有组的加权平均：
```python
strategy_score = mean([group.composite_score for group in groups])
```

## CI 集成

### 回归检测规则

```python
threshold = 5.0  # 可配置

if strategy_score_new < strategy_score_base - threshold:
    FAIL("检测到性能回归")
else:
    PASS
```

**阈值说明：** 5 分差异约相当于 2.5% gap 变化（在 D1 占 40% 权重下）。

### 典型工作流

1. PR 修改策略代码
2. CI 在 baseline + PR 分支运行 benchmark 套件
3. 计算两者的策略评分
4. 比较：`delta = score_pr - score_baseline`
5. 报告：`delta >= -5.0` 则通过，否则失败

### 输出格式

```json
{
  "baseline_score": 78.5,
  "pr_score": 81.2,
  "delta": +2.7,
  "verdict": "PASS",
  "breakdown": {
    "quality": {"baseline": 85.0, "pr": 87.0, "delta": +2.0},
    "anytime": {"baseline": 72.0, "pr": 76.0, "delta": +4.0},
    "efficiency": {"baseline": 78.0, "pr": 79.0, "delta": +1.0},
    "stability": {"baseline": 80.0, "pr": 80.0, "delta": 0.0}
  }
}
```

## 实现

### 计算入口

```bash
# 运行并评分（单次）
python -m benchmarks.run --case tsplib_berlin52 --strategy ga \
  | python -m benchmarks.scoring --mode ci --output ci_score.json

# 批量运行后评分
python -m benchmarks.scoring results/ --mode ci --output ci_score.json
```

### 缺失数据处理

| 缺失项 | 行为 |
|--------|------|
| `reference_cost` | 使用套件最优，标记 `reference_type: "relative"` |
| `incumbent_trace` | 仅从最终 objective 推导（降级 D2 = 0） |
| `candidates_evaluated` | 使用 `total_iterations` 作为代理 |

### 不支持的领域

如果策略不支持某个领域（例如 GA 在 routing 且 `domain_routing_supported=false`）：
- 标记运行为 `N/A`
- 从组/策略聚合中排除
- 在 CI 输出中单独报告

## 示例计算

**输入：**
- TSP 实例，BKS = 7542
- 最终 objective = 7845（gap = 4.0%）
- Primal integral = 1.2（max_integral = 6.0，time_budget = 30s）
- 吞吐率 = 85k evals/s（基线 = 100k/s）
- 终止原因 = `diversity_exhausted`

**评分：**
```python
D1 = max(0, 100 × (1 - 0.04/0.20)) = 80.0
D2 = max(0, 100 × (1 - 1.2/6.0)) = 80.0
D3 = min(100, 100 × log₁₀(85000) / log₁₀(100000)) = 99.2
D4 = 80.0  # diversity_exhausted

综合分 = 0.40×80.0 + 0.30×80.0 + 0.15×99.2 + 0.15×80.0
       = 32.0 + 24.0 + 14.9 + 12.0
       = 82.9
```

## 阈值校准

初始阈值基于工业 benchmark 文献。实际使用后可根据真实数据校准：

```bash
# P75 校准（基于当前运行数据）
python -m benchmarks.scoring --calibrate results/
```

输出各 group 的 P75 gap、P75 throughput，可用于更新硬编码值。

**校准原则：**
- `gap_threshold` = P75 gap（75% 的运行都能达到的 gap 水平）
- `expected_throughput` = median throughput

每季度或重大架构变更后更新。

## 与评估框架的关系

| 特性 | 策略评分（本文档） | 评估框架 |
|------|------------------|----------|
| **目的** | CI 回归检测 | 研究级分析 |
| **输出** | 单一标量 (0–100) | 多维证据链 |
| **运行要求** | 单次运行 | 多 seed（≥5） |
| **统计检验** | 无 | Wilcoxon、Friedman |
| **算子分析** | 无 | 完整算子贡献归因 |
| **可视化** | 简单对比表 | ECDF、雷达图、小提琴图 |
| **决策权** | **控制 CI PASS/FAIL** | 解释性证据，不控制 CI |

**核心区别：**
- 策略评分 = 工程控制系统（快速、确定性、单标量）
- 评估框架 = 科学解释系统（全面、统计、多视角）

## 参考

- 评估框架：`benchmarks/评估框架.md`
- 实现代码：`benchmarks/scoring.py`
- Benchmark 运行器：`benchmarks/run.py`

## 变更日志

- 2026-07-06: 从综合评估框架拆分的 CI 专用版本
